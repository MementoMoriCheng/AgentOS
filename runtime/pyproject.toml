[project]
name = "agentos-runtime"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "grpcio>=1.60",
    "openai>=1.30",
]

[project.scripts]
agentos-run = "agentos_runtime.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
